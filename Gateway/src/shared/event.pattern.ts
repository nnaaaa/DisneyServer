export enum UserPatternEvent {
    CREATE = 'user-create',
    UPDATE = 'user-update',
}

export enum MessagePatternEvent {
    CREATE = 'message-create',
    UPDATE = 'message-update',
    DELETE = 'message-delete',
}
